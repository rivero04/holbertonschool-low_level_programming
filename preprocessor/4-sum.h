#ifndef SUM_H
#define SUM_H

/*
 * Desc: Archivo de encabezado que define una macro similar a una
 * función SUM(x, y) que calcula la suma de los números x e y.
 */

#define SUM(x, y) ((x) + (y))

#endif
